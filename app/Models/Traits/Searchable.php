<?php

namespace App\Models\Traits;

trait Searchable
{
    // public function scopeOrder($query, $inputs)
    // {
    //     if (!isset($inputs['orderBy']) && !isset($inputs['direction'])) {
    //         return $query;
    //     }

    //     return $query->orderBy($inputs['orderBy'], $inputs['direction']);
    // }
}
